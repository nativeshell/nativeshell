# Run with `dart run ffigen --config ffigen.yaml`.
name: NativeAssetsPackageBindings
description: |
  Bindings for `src/native_assets_package.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/native_assets_package_bindings_generated.dart'
headers:
  entry-points:
    - 'src/native_assets_package.h'
  include-directives:
    - 'src/native_assets_package.h'
ffi-native:
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
